spring.application.name=employee-service

# Java Version Requirement
# This application requires Java 25 to run
# Build will fail if Java 25 is not available
# Runtime validation is enforced in build.gradle
